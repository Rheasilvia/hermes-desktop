import type { Component } from 'solid-js';
import { ModelPageContent } from '@/features/model/ModelPageContent.js';

export const ModelPage: Component = () => <ModelPageContent />;

export default ModelPage;
